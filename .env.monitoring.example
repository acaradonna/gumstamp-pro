# Monitoring and Observability Configuration
# Copy this to your .env file and configure as needed

# === SENTRY ERROR TRACKING ===
# Get your DSN from https://sentry.io/settings/projects/
# Free tier: 5,000 errors/month
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# === OPENTELEMETRY CONFIGURATION ===
# For Grafana Cloud (free tier: 10k series, 50GB logs)
# Get credentials from https://grafana.com/auth/sign-up/create-account
OTLP_ENDPOINT=https://otlp-gateway-prod-us-east-0.grafana.net/otlp
GRAFANA_CLOUD_API_KEY=your-base64-encoded-username:api-key

# Alternative: Use Jaeger for local development
# OTLP_ENDPOINT=http://localhost:4317

# === MONITORING CONTROLS ===
# Enable/disable monitoring features
ENABLE_TRACING=true
ENABLE_METRICS=true

# Environment identifier
ENVIRONMENT=production

# === PERFORMANCE THRESHOLDS ===
# Alert thresholds for monitoring
SLOW_REQUEST_THRESHOLD=2.0
ERROR_RATE_THRESHOLD=5.0

# === EXTERNAL MONITORING SERVICES ===
# Better Uptime (free tier: 10 monitors)
# Sign up at https://betteruptime.com/
# Configure HTTP checks for:
# - https://your-app.onrender.com/healthz
# - https://your-app.onrender.com/health

# UptimeRobot (alternative, free tier: 50 monitors)
# Sign up at https://uptimerobot.com/

# === LOG AGGREGATION (OPTIONAL) ===
# For advanced log management, consider:
# - Logtail (free tier: 1GB/month)
# - Papertrail (free tier: 16MB/day, 2 days retention)
# LOGTAIL_SOURCE_TOKEN=your-logtail-token

# === RENDER ENVIRONMENT VARIABLES ===
# Add these in your Render dashboard under Environment Variables:
# 
# Required:
# - SENTRY_DSN
# - GRAFANA_CLOUD_API_KEY
# - OTLP_ENDPOINT
# 
# Optional:
# - ENABLE_TRACING (defaults to true)
# - ENABLE_METRICS (defaults to true) 
# - ENVIRONMENT (defaults to production)
# - SLOW_REQUEST_THRESHOLD (defaults to 2.0)
# - ERROR_RATE_THRESHOLD (defaults to 5.0)